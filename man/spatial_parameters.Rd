% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modules.R
\name{spatial_parameters}
\alias{spatial_parameters}
\title{Define and initialize spatial parameters for a phybreak object}
\usage{
spatial_parameters(
  le,
  dist.model = "power",
  dist.exponent = 2,
  dist.scale = 1,
  dist.mean = 1,
  est.dist.exponent = TRUE,
  prior.dist.exponent.shape = 1,
  prior.dist.exponent.mean = 1,
  est.dist.scale = TRUE,
  prior.dist.scale.shape = 1,
  prior.dist.scale.mean = 1,
  est.dist.mean = TRUE,
  prior.dist.mean.shape = 1,
  prior.dist.mean.mean = 1
)
}
\arguments{
\item{le}{A \code{phybreak} object given by the \code{phybreak} function. 
The \code{phybreak} object should contain \code{locations} and/or \code{distances}.}

\item{dist.model}{A character string specifying the spatial distance model. Options
include \code{"power"}, \code{"exponential"}, or \code{"poisson"} (default = \code{"power"}).}

\item{dist.exponent}{Initial value for the distance exponent in the spatial model
(default = 2).}

\item{dist.scale}{Initial scale parameter for the distance model (default = 1).}

\item{dist.mean}{Initial mean parameter, used if \code{dist.model = "poisson"}
(default = 1).}

\item{est.dist.exponent}{Logical; whether to estimate the distance exponent (default = \code{TRUE}).}

\item{prior.dist.exponent.shape, prior.dist.exponent.mean}{Shape and mean of the prior
(Gamma) distribution for the distance exponent (default = 1, 1).}

\item{est.dist.scale}{Logical; whether to estimate the distance scale (default = \code{TRUE}).}

\item{prior.dist.scale.shape, prior.dist.scale.mean}{Shape and mean of the prior
(Gamma) distribution for the distance scale (default = 1, 1).}

\item{est.dist.mean}{Logical; whether to estimate the distance mean (only relevant for
\code{dist.model = "poisson"}; default = \code{TRUE}).}

\item{prior.dist.mean.shape, prior.dist.mean.mean}{Shape and mean of the prior
(Gamma) distribution for the distance mean (default = 1, 1).}

\item{spatial}{Logical; whether to include spatial parameters in the model (default = \code{FALSE}).}
}
\value{
The input object \code{le}, extended with:
\itemize{
  \item \strong{dataslot}: containing locations, distances, and computed area (if available).
  \item \strong{parameterslot}: containing spatial model type and initial values for
  distance parameters.
  \item \strong{helperslot}: containing estimation flags and prior values for spatial
  parameters.
  \item \strong{sampleslot}: empty containers for posterior samples of spatial parameters.
}
}
\description{
This function extends a \code{phybreak} object within the \code{phybreak} function with
spatial information and corresponding model parameters. It sets up the spatial model,
attaches locations and/or distances to the data slot, initializes priors for parameter
estimation, and prepares empty sample containers for MCMC output.
}
\details{
If only distances are provided in the dataset, the function uses multidimensional scaling
(\code{cmdscale}) to approximate locations. The area of the spatial domain is computed
from the range of the provided or derived coordinates.
}
\examples{
# Example: add spatial parameters to a phybreak-like object
MCMCstate <- phybreak(dataset, spatial = TRUE, dist.model = "power")

}
\seealso{
\code{\link{phybreak}}, \code{\link{contact_parameters}}
}
